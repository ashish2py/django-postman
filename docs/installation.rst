============
Installation
============

At the command line::

    $ easy_install django-postman

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-postman
    $ pip install django-postman
